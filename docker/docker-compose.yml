services:
  web:
    build:
      context: ..
      dockerfile: docker/django/Dockerfile
    container_name: web
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - shared_net
    volumes:
      - ${STATIC_FILES_DIR}:/app/staticfiles
      - ${MEDIA_FILES_DIR}:/app/media

  nginx:
    build:
      context: ./nginx
    container_name: nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT:-80}:80"
    depends_on:
      - web
    environment:
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
    networks:
      - shared_net
    volumes:
      - ${STATIC_FILES_DIR}:/app/staticfiles:ro
      - ${MEDIA_FILES_DIR}:/app/media:ro

networks:
  shared_net:
    external: true
